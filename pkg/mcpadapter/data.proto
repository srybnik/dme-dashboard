syntax = "proto3";


package api;
option go_package = "../mcpadapter";

service StreamService {
  rpc Stream (stream Data) returns (stream Data) {}
}

message Data {
  int32 device = 1;
  int32 pin = 2;
  bool value = 3;
  bool hasErr = 4;
  int32 mode = 5;
 }
